# Runtime dependencies only (all pure-Python / stdlib + scientific stack).
# fetch.py uses urllib from the standard library (no `requests` needed).
pandas>=2.0
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
# Paper-trading runner only (run_paper.py, Mac Mini side; NOT needed for backtests):
# ib_insync>=0.9  (pip install ib_insync)
